// @ts-nocheck — scaffold template; placeholder imports resolve once copied into a package.
// Barrel rule: export the component + its types ONLY. Never export styles. Never use `export *`.
export { MyThing } from "./component-name";
export type { MyThingProps } from "./component-name.types";
