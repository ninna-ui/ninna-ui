---
"@ninna-ui/forms": patch
---

fix(forms): properly apply visual color payloads and dynamic selection state to `RadioGroupItem` and `RadioCard` inner indicators to ensure visibly colored checked states.
