/**
 * Shared form component types
 * Derives from core types for consistency across the design system.
 */

import type { CompactSize } from '@ninna-ui/core';

/** Checkbox size options */
export type CheckboxSize = CompactSize;

/** Switch size options */
export type SwitchSize = CompactSize;

/** Radio size options */
export type RadioSize = CompactSize;

/** Slider size options */
export type SliderSize = CompactSize;
