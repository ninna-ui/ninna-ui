import { NINNA_UI_VERSION } from "~/constants/version";
import type { ComponentMeta } from "~/components/docs/types";

export const linkMeta: ComponentMeta = {
  title: "Link",
  description: "Link component is used to render accessible anchor elements with various styles, colors, and underline behaviors. It supports external links with automatic security attributes.",
  category: "Primitives",
  version: NINNA_UI_VERSION,
  status: "stable",
};

