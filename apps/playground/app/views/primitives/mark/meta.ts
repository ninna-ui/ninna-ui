import { NINNA_UI_VERSION } from "~/constants/version";
import type { ComponentMeta } from "~/components/docs/types";

export const markMeta: ComponentMeta = {
  title: "Mark",
  description: "Mark component is used to highlight text with a background color. It uses the semantic HTML mark element for proper accessibility.",
  category: "Primitives",
  version: NINNA_UI_VERSION,
  status: "stable",
};

