import { NINNA_UI_VERSION } from "~/constants/version";
import type { ComponentMeta } from "~/components/docs/types";

export const iconButtonMeta: ComponentMeta = {
  title: "IconButton",
  description: "IconButton is a button component designed for icon-only actions. It requires an aria-label for accessibility and supports all the same variants, colors, and sizes as the regular Button component.",
  category: "Primitives",
  version: NINNA_UI_VERSION,
  status: "stable",
};

