import { NINNA_UI_VERSION } from "~/constants/version";
import type { ComponentMeta } from "~/components/docs/types";

export const switchMeta: ComponentMeta = {
  title: "Switch",
  description: "Switch component for toggling between two states with support for labels, descriptions, and multiple sizes.",
  category: "Forms",
  version: NINNA_UI_VERSION,
  status: "stable",
};

